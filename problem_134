#include <stdio.h>

int main() {
    FILE *fp;
    char ch;

    fp = tmpfile();
    if (fp == NULL) return 1;

    fprintf(fp, "This is a temporary file example.\n");
    rewind(fp);

    while ((ch = fgetc(fp)) != EOF)
        putchar(ch);

    fclose(fp);
    return 0;
}
